# Keepa CSV

Keepa の Product Finder から書き出した CSV をここに置きます。

使う列は梱包サイズと重量だけです（他の列があっても構いません）。

- `ASIN`
- `Parent ASIN`（任意・親ASINでも梱包サイズを引けます）
- `Package: Length (cm)`
- `Package: Width (cm)`
- `Package: Height (cm)`
- `Package: Weight (g)`

`Buy Box: Current` や `Amazon: Current` などの売価は計算に使いません。
